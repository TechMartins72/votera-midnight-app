import React from "react";

const Button = () => {
  return (
    <button className="w-full bg-red-700 text-white rounded-2xl uppercase font-bold cursor-pointer py-2 hover:bg-red-600 transition-all duration-200">
      Vote
    </button>
  );
};

export default Button;
